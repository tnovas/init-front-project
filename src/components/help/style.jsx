
const style = (theme) => ({
    paper: {
        padding: theme.spacing.unit * 2,
        textAlign: 'center'
    },
    typography: {
        color: theme.palette.text.secondary
    }
});

export default style;